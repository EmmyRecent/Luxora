export type sidebarMenuItemsType = {
  to: string;
  label: string;
  icon: string;
};

export type dashboardStatDataType = {
  icon: string;
  title: string;
  description: string;
  stat: number;
};

export type dashboardStatsType = {
  icon: string;
  title: string;
  description: string;
  stat: number;
};
